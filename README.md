# 서울시 지하철 접근성 소외지역 시각화 분석 및 후보지 도출 시스템 설계

> 빅데이터 프로그래밍 기말 프로젝트
> 학번: 60220889
> 성명: 김명관

---

# 1. 프로젝트 개요 및 문제 정의

현재 수도권의 대중교통망은 외형적으로 잘 구축되어 있으나, 실제 생활인구 및 유동인구의 분포와 비교할 때 지하철 접근성이 현저히 떨어지는 교통 소외 지역이 존재한다. 특히 명지대학교 인근과 같이 대중교통 의존도가 높고 등하교 시간대 상습적인 혼잡을 겪는 지역이 대표적이다.

본 프로젝트는 서울시 전역의 다차원 데이터(생활인구, 정류장별 승하차 인원, 지하철 역사 위치 등)를 통합 분석하여 인구 밀집도 대비 지하철 접근성이 낮은 지역을 정량적으로 식별하고, 신규 지하철역 입지 타당성을 검토할 수 있는 시각화 분석 시스템을 설계하는 것을 목표로 한다.

## 핵심 분석 질문

1. **[공간]** 해당 정류장이 지하철역으로부터 멀리 떨어진 물리적 소외 지역에 해당하는가?
2. **[실수요]** 해당 정류장의 실제 대중교통(버스) 일일 이용 수요는 높은가?
3. **[잠재수요]** 해당 정류장이 속한 행정동의 상시 생활인구 밀집도는 높은가?
4. **[인사이트]** 도출된 교통 소외 정류장들이 특정 행정동에 뚜렷하게 군집화(Clustering)되어 나타나는가?

---

# 2. 시스템 아키텍처 및 기술 스택

대용량 시계열 데이터를 안정적으로 처리하고 공간 연산의 복잡도를 분산하기 위해 Hadoop-Spark-Hive 기반의 분석 파이프라인을 구축하였다.

## 기술 스택

| 구분             | 기술                                      |
| -------------- | --------------------------------------- |
| OS / Platform  | Linux (HDP)                             |
| Storage        | HDFS                                    |
| Processing     | Apache Spark (DataFrame API, Spark SQL) |
| Data Warehouse | Apache Hive                             |
| Language       | Python (PySpark, PyProj)                |
| Automation     | Bash Script                             |
| Visualization  | Plotly                                  |

## 데이터 파이프라인

### 1. Ingestion

* `data.py`
* 서울 열린데이터 광장 API 데이터 수집
* HDFS 적재

### 2. Processing

* `join_data.py`

* 데이터 정제 및 Join

* `convert.py`

* 좌표계 변환

* EPSG:5181 → EPSG:4326

### 3. Analytics

* `analysis.py`
* Spark SQL 기반 공간 분석
* 정류장 ↔ 지하철역 최단거리 계산

### 4. Serving

* `run_pipeline.sh`
* Hive External Table 생성
* 상위 후보지 집계 및 조회

---



# 3. 데이터셋

모든 데이터는 서울 열린데이터 광장 Open API를 통해 자동 수집하였다.

## 사용 데이터

* 행정동 단위 서울 생활인구 (내국인)

  * `seoul_living_pop_dong_2025.csv`

* 서울시 상권분석서비스 (영역-행정동)

  * `dong_area.csv`

* 서울시 역사 마스터 정보

  * `subway_station_coords.csv`

* 서울시 버스노선별 정류장별 시간대별 승하차 인원

  * `bus_time_data_2025.csv`

* 서울시 버스정류소 위치정보

  * `bus_stop_coords.csv`

---

# 4. 실행 가이드 
(HDFS /user/maria_dev/project 경로 존재 필수)

## Step 1. 환경 변수 설정

프로젝트 루트에 `.env` 파일 생성 (서울 열린데이터 광장 인증키 입력)

```bash
echo 'SEOUL_API_KEY="YOUR_API_KEY"' > .env
```

## Step 2. 실행 권한 부여

```bash
cd src/pipeline
chmod +x run_pipeline.sh
```

## Step 3. 전체 파이프라인 실행

```bash
./run_pipeline.sh
```

실행 결과:

```text
final_top100_report.csv
```

생성(시각화 실행할 로컬 환경으로 다운로드 필요함)

## Step 4. 시각화 실행(vscode 등 외부 환경에서 실행)

```bash
python plot.py
```

---

# 5. 분석 결과 및 인사이트

 단순히 거리가 멀고 인구가 많은 지역을 나열하는 것을 넘어, 데이터 스케일의 극단적 차이로 인한 통계적 왜곡을 방지하기 위해 백분위수 정규화 기반의 가중 선형 조합 모델을 설계하였다.

### 1. 단위 정규화

대중교통 승하차 인원과 특정 행정동의 생활인구는 강남역 등 일부 거점 지역에 데이터가 집중되는 분포(Power-law Distribution)를 따른다. 따라서 극단적인 이상치(Outlier)의 영향을 줄이기 위해 전체 데이터 내 상대적 순위만을 활용하는 백분위수 정규화를 적용하였다.

$$
\hat{X}_{i} = \frac{Rank(X_{i}) - 1}{N - 1}
$$

변수 정의

- $\hat{X}_{i}$ : 0~1 범위로 정규화된 지수
- $X_{i}$ : 정류장 $i$의 원본 데이터 값 (생활인구, 승하차 인원, 거리)
- $N$ : 전체 분석 대상 버스 정류장 수

---

### 2. 최종 교통 취약 지수 산출

정규화된 각 지표(생활인구, 승하차 수요, 지하철역과의 거리)에 가중치를 부여하여 최종 교통 취약 지수(Score)를 산출하였다.


$$ Score_i =
(w_p \cdot \hat{P}_i)
+
(w_b \cdot \hat{B}_i)
+
(w_d \cdot \hat{D}_i)
$$

변수 정의

- $\hat{P}_{i}$ : 정류장 $i$의 생활인구 백분위수 지수 (잠재 수요)
- $\hat{B}_{i}$ : 정류장 $i$의 승하차 인원 백분위수 지수 (실제 수요)
- $\hat{D}_{i}$ : 정류장 $i$의 지하철역 이격 거리 백분위수 지수 (물리적 소외도)
- $w_p, w_b, w_d$ : 각 지표의 가중치

가중치 제약 조건

$$
w_p + w_b + w_d = 1.0
$$

본 프로젝트에서는 다음과 같이 설정하였다.

$$
w_p = 0.3,\quad
w_b = 0.4,\quad
w_d = 0.3
$$

가중치의 경우 세 변수의 중요도를 단순히 1:1:1로 설정할 수도 있었으나, **잠재수요(생활인구)보다 실수요(버스 승하차 인원)가 대중교통 사각지대를 판단하는 데 더 직접적인 지표**라고 판단하였다. 생활인구가 많더라도 자가용 이용률이 높을 수 있기 때문에 버스 승하차 인원에 상대적으로 높은 가중치(0.4)를 부여하였다.

본 점수는 다음 세 가지 요소를 동시에 반영한다.

1. 생활인구가 많을수록 더 많은 시민이 영향을 받는다.
2. 버스 이용 수요가 많을수록 대중교통 의존도가 높다.
3. 지하철역과의 거리가 멀수록 접근성이 낮다.

따라서 Score가 높은 지역은 많은 생활인구와 높은 버스 수요를 보유하면서도 지하철 접근성이 부족한 지역으로 해석할 수 있으며, 신규 지하철역 또는 대중교통 인프라 확충이 우선적으로 검토되어야 할 잠재 후보지로 판단하였다.

  
## 시각화 기준

### 버블 크기

* `daily_total_on`
* 일일 총 승차 인원

### 색상

* `robust_score`
* 교통 취약 지수

교통 취약 지수가 높을수록 붉은색으로 표현된다.

## 핵심 인사이트

분석 결과 특정 행정동 단위에서 다음 특징이 확인되었다.

* 높은 생활인구
* 높은 버스 이용량
* 낮은 지하철 접근성

특히 교통 소외 정류장이 특정 지역에 군집화되는 현상이 관찰되었으며, 이는 신규 지하철역 또는 경전철 환승 거점 검토 시 활용 가능한 데이터 기반 근거를 제공한다.

---

# 6. AI Tool Usage

본 프로젝트에서는 다음과 같은 AI 도구를 활용하였다.

### Gemini 3.1 Pro

*프로젝트명 아이디어 참고, 하둡 분산 환경 Spark UDF 및 pyproj 라이브러리 의존성 에러 디버깅, 시각화 레이아웃(Plotly Subplots) 아이디어 보조.
